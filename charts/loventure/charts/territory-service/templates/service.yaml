apiVersion: v1
kind: Service
metadata:
  name: {{ include "territory-service.fullname" . }}
  labels:
    {{- include "territory-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "territory-service.selectorLabels" . | nindent 4 }}

